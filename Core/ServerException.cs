using System;
using System.Collections.Generic;
using System.Text;

namespace dotnet_server.Core
{
    public class ServerException : Exception
    {
        public void Error()
        {
            
        }

        /*
        public Boolean Check(object key , Dictionary<string , T> vals)
        {
            if(key != null && key.ToString() != string.Empty)
            {
                if(vals.)
            }
            return false;
        }
        */

    }
}
